@model GorevYoneticisi.Models.Gorev

@{
    ViewBag.Title = "Delete";
}

<h3 class="ortala">Silmek istediğinize emin misiniz?</h3>
<div>
    <div class="card">
        <hr />
        <dl class="dl-horizontal">
            <dt class="card-header">
                @Html.DisplayNameFor(model => model.GorevAdi)
            </dt>

            <dd class="card-header">
                @Html.DisplayFor(model => model.GorevAdi)
            </dd>

            <dt class="card-body">
                @Html.DisplayNameFor(model => model.Aciklama)
            </dt>

            <dd class="card-body">
                @Html.DisplayFor(model => model.Aciklama)
            </dd>

            <dt class="card-body2">
                @Html.DisplayNameFor(model => model.Durum)
            </dt>

            <dd class="card-body2">
                @Html.DisplayFor(model => model.Durum)
            </dd>

            <dt class="card-footer">
                @Html.DisplayNameFor(model => model.DueDate)
            </dt>

            <dd class="card-footer">
                @Html.DisplayFor(model => model.DueDate)
            </dd>

        </dl>
    </div>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

<div class="form-actions no-color ortala">
    <input type="submit" value="Sil" class="btn btn-primary" /> |
    <a href="@Url.Action("Index", "Gorev")" class="btn btn-primary">Görev Listesine Dön</a>
</div>           
            }
        </div>
